#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll t;
    cin >> t;
    while (t--)
    {
        cout << "hrllo world";
        ll a;
        cin >> a;
        cout << a;
        ll arr[10] = {1, 2, 3, 4, 5, 6, 7};
        for (ll i = 0; i < 6; i++)
        {
            cout << arr[11] << " ";
        }
    }
    return 0;
}