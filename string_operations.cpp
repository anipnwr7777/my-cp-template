#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;

int main()
{
     ios::sync_with_stdio(0);
     cin.tie(0);
     // ll t;
     // cin >> t;
     // while (t--)
     {
          // convert any integer or char to string and
          // then extend that string forward.

          // char a = 'a';
          // stringstream stream;
          // stream << a;
          // string x = stream.str();
          // x += "1a.*";
          // cout << x << "\n";

          // int b = 1;
          // stringstream stream2;
          // stream2 << b;
          // string y = stream2.str();
          // y += "2";
          // cout << y << "\n";

          // x.erase(0, 1);
          // cout << x << "\n";

          // int d = 98;
          // char e = d;
          // cout << e << "\n";

          // char i = char(d);
          // cout << i << "\n";

          // char f = 'c';
          // int g = f;
          // cout << g << "\n";

          // int j = 2;
          // float k = 2.1;
          // k += j;
          // j += k;
          // cout << k << " " << j << "\n";

          // store decimal in integer to convert it
          // to integer

          //if you want it should not be changed
          //to integer, then store it in float

          // int l = 22;
          // float m = 0.000011;
          // cout << m << "\n";
          // m += l;
          // cout << m << "\n";

          // int l = 22;
          // float m = 0.010101;
          // cout << m << "\n";
          // m += l;
          // cout << m << "\n";

          // by default setprecusion is 6
          // cout << setprecision(10);
          // ld a = 22222222.222222;
          // cout << a << "\n";

          // cout << setprecision(5);
          // cout << a << "\n";

          // ld r = 5.0;
          // cout << r << "\n";

          // ld s = 22.0 / 7.0;
          // cout << s << "\n";

          // ld t = 22 / 7;
          // cout << t << "\n";

          // ld v = 22.0 / 7;
          // cout << v << "\n";

          // ld u = 22.1 / 7;
          // cout << u << "\n";

          // ld x = 22.0;
          // ll y = 7;
          // ld ans = x / y;
          // cout << ans << "\n";

          // ld z = 2.11111111181111111118;
          // cout << setprecision(21) << z << "\n";

          // ld z1 = 2.22113344;
          // cout << setprecision(7) << z1 << "\n";
          // cout << fixed << setprecision(7) << z1 << "\n";

          // string test = "654 1";
          // stringstream stream;
          // stream << test;
          // int x;
          // int ans = 0;
          // while (!stream.eof())
          // {
          //     stream >> x;
          //     ans += x;
          // }
          // cout << ans << "\n";

          // string test = "654 1";
          // stringstream stream;
          // stream << test;
          // int x;
          // int ans = 0;
          // while (stream >> x)
          // {
          //     ans += x;
          // }
          // cout << ans << "\n";

          // int co = 2;
          // int len = 5;
          // string ans = to_string(co);
          // ans += "/";
          // ans += to_string(len);
          // cout << ans << "\n";

          //string s;
          //reverse(s.begin(),s.end());
          //we can't equate this to some
          //other string. string s will be
          //reversed in itself.

          // getline and cin.getline
          // string s;
          // getline(cin, s, 'b');
          // cout << s;

          // null cahr is apppended own its own. give
          // one extra length
          // char arr[20];
          // cin.getline(arr, 7 + 1);
          // for (auto x : arr)
          // {
          //     cout << x << " ";
          // }
          // cout << "\n";
          // for (ll i = 0; i < 20; i++)
          // {
          //     if (arr[i] == '\0')
          //     {
          //         break;
          //     }
          //     cout << arr[i];
          // }

          // char arr[20];
          // cin.getline(arr, 7 + 1, '2');
          // for (auto x : arr)
          // {
          //     cout << x << " ";
          // }
          // cout << "\n";
          // char ass[10];
          // cin.getline(ass, 3 + 1);
          // for (auto x : ass)
          // {
          //     cout << x << " ";
          // }

          // string s;
          // getline(cin, s);
          // getline(cin, s);
          // cout << s << "\n";

          char a = '4';
          int ia = a - '0';
          cout << ia;
     }
     return 0;
}